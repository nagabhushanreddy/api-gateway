# API Gateway Service

Entry point for all client requests. Handles routing, authentication, rate limiting, and cross-cutting concerns.

## Status
🚧 **Structure Created** - Implementation pending

## Endpoints (Planned)
- `/health` - Health check
- `/api/v1/discovery/` - Service discovery
- `/api/v1/auth/*` - Route to auth service
- `/api/v1/users/*` - Route to profile service
- `/api/v1/loans/*` - Route to loan service
- `/api/v1/documents/*` - Route to document service
- `/api/v1/notifications/*` - Route to notification service
- `/api/v1/audit/*` - Route to audit service
